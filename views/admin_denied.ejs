<!DOCTYPE html>
<html><head>
<meta charset="utf-8"><title>Denied Students – Admin</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head><body class="bg-light">
<div class="container py-5">
  <h1 class="mb-4">Denied Student Registrations</h1>
  <% if (!denied.length) { %>
    <div class="alert alert-info">No denied students.</div>
  <% } else { %>
  <table class="table table-striped align-middle">
    <thead><tr><th>Name</th><th>Username</th><th>Email</th><th>Student ID</th><th>Details</th></tr></thead>
    <tbody>
      <% denied.forEach(d => { %>
        <tr>
          <td><%= d.name %></td>
          <td><%= d.username %></td>
          <td><%= d.email %></td>
          <td><%= d.profile?.studentId || '-' %></td>
          <td><a class="btn btn-sm btn-outline-secondary" href="/admin/students/<%= d.id %>">View</a></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
  <% } %>
  <a href="/admin" class="btn btn-link">Back to dashboard</a>
</div>
</body></html>