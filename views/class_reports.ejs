<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= klass.name %> – Student Sheet</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <%- include('header', { user: { role: scope } }) %>
  <div class="container py-4">
    <h2><%= klass.name %> – Student Sheet</h2>
    <table class="table table-bordered">
      <thead>
        <tr>
          <th>Student</th>
          <% (klass.tests || []).forEach(t => { %>
            <th><%= t.title %></th>
          <% }) %>
        </tr>
      </thead>
      <tbody>
        <% (students || []).forEach(s => { %>
          <tr>
            <td><%= s.name %></td>
            <% (klass.tests || []).forEach(t => { %>
              <% const g = (klass.grades || []).find(gr => gr.testId === t.id && gr.studentId === s.id); %>
              <td><%= g ? (g.score + '%') : '—' %></td>
            <% }) %>
          </tr>
        <% }) %>
      </tbody>
    </table>
    <a href="javascript:history.back()" class="btn btn-link">Back</a>
  </div>
</body>
</html>