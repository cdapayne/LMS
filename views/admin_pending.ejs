<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Approvals – Admin | MD Technical School</title>
  <link rel="icon" href="<%= branding?.favicon %>">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    :root{
      --primary-color: dodgerblue;
      --secondary-color: orange;
      --bg: #f4f6f9;
      --surface: #ffffff;
      --border: #e5e7eb;
      --muted: #6b7280;
    }
    body{ background: var(--bg); font-family: "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif; }

 .page-head{
  background: var(--primary-color);  /* solid dodgerblue */
  color: black;
  border-bottom: 1px solid var(--border);
  padding: 1rem 0;
  margin-bottom: 1rem;
}
.page-head h1 { margin: 0; font-size: 1.35rem; font-weight: 800; color:#fff; }
    .pill{
      display:inline-block; background:#fff; border:1px solid var(--border);
      border-radius:999px; padding:.35rem .7rem; font-weight:600;
      box-shadow: 0 .3rem .8rem rgba(0,0,0,.04);
    }

    .cardish{
      background: var(--surface);
      border:1px solid var(--border);
      border-radius:12px;
      box-shadow: 0 .6rem 1.2rem rgba(0,0,0,.05);
      padding:1rem;
    }
    .section-title{
      display:flex; align-items:center; gap:10px; font-weight:700; margin-bottom:.75rem;
    }
    .section-title::before{ content:""; width:6px; height:18px; border-radius:3px; background: var(--secondary-color); }

    .btn-primary{ background: var(--primary-color) !important; border-color: var(--primary-color) !important; }
    .btn-primary:hover{ background:#1e6edb !important; border-color:#1e6edb !important; }
    .btn-outline-primary{ color: var(--primary-color) !important; border-color: var(--primary-color) !important; }
    .btn-outline-primary:hover{ background: var(--primary-color) !important; color:#fff !important; }

    .table-wrap{ overflow:auto; max-height: 70vh; border:1px solid var(--border); border-radius:10px; }
    .table thead th{ background:#f8fafc; font-weight:600; position:sticky; top:0; z-index:1; }
    .table-bordered{ border-color: var(--border) !important; }
    .table-bordered > :not(caption) > * { border-color: var(--border) !important; }

    .tools-bar{ gap:.5rem; }
    .muted{ color: var(--muted); }
  </style>
</head>
<body>
  <% if (typeof user !== 'undefined' && user) { %>
    <%- include('header', { user: user }) %>
  <% } %>

  <!-- Page Head -->
  <div class="page-head">
    <div class="container d-flex flex-wrap justify-content-between align-items-end gap-2">
      <div>
        <h1>Pending Student Approvals</h1>
        <div class="mt-2">
          <span class="pill">Pending: <%= pending.length %></span>
        </div>
      </div>
      <% if (pending.length) { %>
      <div class="tools-bar d-flex">
        <input id="searchInput" class="form-control" placeholder="Search name/username/email…" />
        <button id="clearSearch" class="btn btn-outline-secondary">Clear</button>
        <button id="exportCsv" class="btn btn-primary">Export CSV</button>
      </div>
      <% } %>
    </div>
  </div>

  <div class="container pb-4">
    <% if (!pending.length) { %>
      <div class="cardish">
        <div class="alert alert-success m-0">No pending registrations.</div>
      </div>
      <div class="d-flex justify-content-between align-items-center mt-3">
          <div class="muted small">Showing <span id="rowCount"><%= pending.length %></span> students</div>
          <a href="/admin" class="btn btn-link">Back to dashboard</a>
        </div>
    <% } else { %>
      <div class="cardish">
        <div class="section-title">Awaiting Review</div>
        <div class="table-wrap">
          <table id="approvalsTable" class="table table-bordered table-hover align-middle mb-0">
            <thead>
              <tr>
                <th style="min-width:220px;">Name</th>
                <th style="min-width:140px;">Username</th>
                <th style="min-width:220px;">Email</th>
                <th style="min-width:120px;">Applied</th>
                <th style="min-width:160px;">Course</th>
                <th style="min-width:160px;">Program</th>
                <th style="min-width:160px;">Actions</th>
              </tr>
            </thead>
            <tbody>
              <% pending.forEach(p => { %>
                <tr>
                  <td><%= p.name %></td>
                  <td><%= p.username %></td>
                  <td><%= p.email %></td>
                  <td><%= p.appliedAt ? new Date(p.appliedAt).toLocaleDateString() : '-' %></td>
                  <td><%= p.profile?.course || '-' %></td>
                  <td><%= p.profile?.affiliateProgram || '-' %></td>
                  <td>
                    <div class="d-flex flex-wrap gap-2">
                      <a class="btn btn-sm btn-outline-secondary" href="/admin/students/<%= p.id %>">View</a>
                      <form method="post" action="/admin/approve/<%= p.id %>" onsubmit="return confirm('Approve this student?');">
                        <button class="btn btn-sm btn-success">Approve</button>
                      </form>
                      <form method="post" action="/admin/decline/<%= p.id %>" onsubmit="return confirm('Decline this student?');">
                        <button class="btn btn-sm btn-danger">Decline</button>
                      </form>
                    </div>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>

        <div class="d-flex justify-content-between align-items-center mt-3">
          <div class="muted small">Showing <span id="rowCount"><%= pending.length %></span> students</div>
          <a href="/admin" class="btn btn-link">Back to dashboard</a>
        </div>
      </div>
    <% } %>
  </div>

  <% if (pending.length) { %>
  <script>
    (function(){
      const input = document.getElementById('searchInput');
      const clearBtn = document.getElementById('clearSearch');
      const table = document.getElementById('approvalsTable');
      const rowCountEl = document.getElementById('rowCount');
      const exportBtn = document.getElementById('exportCsv');

      function normalize(s){ return (s||'').toString().toLowerCase(); }

      function filterTable(){
        const q = normalize(input.value);
        let shown = 0;
        Array.from(table.tBodies[0].rows).forEach(tr => {
          const name = normalize(tr.cells[0]?.innerText);
          const uname = normalize(tr.cells[1]?.innerText);
          const email = normalize(tr.cells[2]?.innerText);
          const match = !q || name.includes(q) || uname.includes(q) || email.includes(q);
          tr.style.display = match ? '' : 'none';
          if (match) shown++;
        });
        rowCountEl.textContent = shown;
      }

      function toCsv(){
        const rows = [];
        const visibleRows = Array.from(table.querySelectorAll('tr'))
          .filter(tr => tr.closest('tbody') ? tr.style.display !== 'none' : true);

        visibleRows.forEach(tr => {
          const cells = Array.from(tr.cells).map(td => {
            const t = td.innerText.replace(/\r?\n+/g,' ').trim();
            return /[",]/.test(t) ? '"' + t.replace(/"/g,'""') + '"' : t;
          });
          rows.push(cells.join(','));
        });

        const blob = new Blob([rows.join('\n')], {type:'text/csv;charset=utf-8;'});
        const url = URL.createObjectURL(blob);
        const a = document.createElement('a');
        a.href = url; a.download = 'pending_approvals.csv'; a.click();
        URL.revokeObjectURL(url);
      }

      input?.addEventListener('input', filterTable);
      clearBtn?.addEventListener('click', () => { input.value=''; filterTable(); });
      exportBtn?.addEventListener('click', toCsv);
    })();
  </script>
  <% } %>
</body>
</html>
