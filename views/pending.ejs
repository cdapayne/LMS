<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<title>Registration Pending</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container py-5 text-center">
    <h1>
      Thanks, 
      <%= user?.name 
          || ((user?.firstName || '') + ' ' + (user?.lastName || '')).trim() 
          || 'New Student' %>!
    </h1>
    <p>Your registration is <strong>pending admin approval</strong>. You’ll get an email when it’s processed.</p>
    <a class="btn btn-primary mt-3" href="/login">Back to Login</a>
  </div>
</body>
</html>