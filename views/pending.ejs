<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Registration Pending – MD Technical School</title>
  <link rel="icon" href="<%= branding?.favicon %>">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    :root{
      --primary-color: dodgerblue;
      --secondary-color: orange;
      --bg: #f4f6f9;
      --surface: #ffffff;
      --border: #e5e7eb;
      --muted: #6b7280;
    }
    body{ background: var(--bg); font-family: "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif; }

    /* Hero */
    .hero{
      background: linear-gradient(180deg, rgba(30,144,255,.10), rgba(255,165,0,.06));
      border-bottom: 1px solid var(--border);
      padding: 2rem 0;
      margin-bottom: 1.25rem;
    }
    .hero h1{ margin:0; font-weight:800; font-size:1.5rem; }

    /* Status card */
    .cardish{
      background: var(--surface);
      border:1px solid var(--border);
      border-radius:12px;
      box-shadow: 0 .6rem 1.2rem rgba(0,0,0,.06);
      padding:2rem 1.5rem;
      max-width: 720px;
      margin: 0 auto;
      text-align:center;
    }

    .status-icon{
      width:64px; height:64px; border-radius:50%;
      display:inline-flex; align-items:center; justify-content:center;
      background:#e7f1ff; /* light primary */
      color: var(--primary-color);
      margin-bottom: .75rem;
      box-shadow: 0 .3rem .9rem rgba(30,144,255,.15);
    }

    .lead{
      font-size:1.05rem;
    }
    .muted{ color: var(--muted); }

    .btn-primary{ background: var(--primary-color) !important; border-color: var(--primary-color) !important; }
    .btn-primary:hover{ background:#1e6edb !important; border-color:#1e6edb !important; }
    .btn-outline-primary{ color: var(--primary-color) !important; border-color: var(--primary-color) !important; }
    .btn-outline-primary:hover{ background: var(--primary-color) !important; color:#fff !important; }

    .aid-box{
      background:#fff;
      border:1px dashed var(--border);
      border-radius:10px;
      padding: .9rem;
      margin-top: 1rem;
    }
    .aid-box a{ text-decoration: none; }
    .aid-badge{
      background: #fff7ed; border: 1px solid #fed7aa; color:#9a3412;
      font-weight:600; border-radius:999px; padding:.2rem .6rem; font-size:.85rem;
      margin-right:.5rem;
    }
  </style>
</head>
<body>
  <% if (typeof user !== 'undefined' && user) { %>
    <%- include('header', { user: user }) %>
  <% } %>

  <!-- Hero -->
  <div class="hero">
    <div class="container">
      <h1>Registration Pending</h1>
      <div class="muted">Thank you for completing your application.</div>
    </div>
  </div>
ƒ
  <div class="container pb-5">
    <div class="cardish">
      <div class="status-icon" aria-hidden="true">
        <!-- check-circle icon -->
        <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" fill="currentColor" viewBox="0 0 16 16" role="img" aria-label="Submitted">
          <path d="M8 15A7 7 0 1 1 8 1a7 7 0 0 1 0 14zm3.97-8.03a.75.75 0 0 0-1.06-1.06L7 9.81 5.53 8.34a.75.75 0 1 0-1.06 1.06L6.47 11.4a.75.75 0 0 0 1.06 0l4.44-4.43z"/>
        </svg>
      </div>

      <h2 class="mb-2">
        Thanks,
        <%= user?.name
            || ((user?.firstName || '') + ' ' + (user?.lastName || '')).trim()
            || 'New Student' %>!
      </h2>

      <p class="lead">
        Your registration is <strong>pending admin approval</strong>.
      </p>
      <p class="muted mb-0">We’ll send you an email as soon as it’s processed.</p>

      <% const showFinancialAid = typeof financialAid !== 'undefined' ? financialAid : user?.profile?.financialAidRequested; %>
      <% if (showFinancialAid) { %>
        <div class="aid-box">
          <span class="aid-badge">Financial Aid</span>
          If you haven’t already, you can begin your application on
          <a class="btn btn-sm btn-outline-primary ms-2" href="https://studentaid.gov" target="_blank" rel="noopener">studentaid.gov</a>
        </div>
      <% } %>

        <% if (selfPay) { %>
        <div class="mt-3">
          <a class="btn btn-outline-primary" href="/pay">Make a Payment</a>
        </div>
      <% } %>


      <div class="mt-3">
        <a class="btn btn-primary" href="/login">Back to Login</a>
        <a class="btn btn-link" href="javascript:history.back()">Go Back</a>
      </div>
    </div>
  </div>
</body>
</html>
