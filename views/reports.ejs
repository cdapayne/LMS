<!DOCTYPE html>
<html><head>
<meta charset="utf-8"><title>Reports</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head><body class="bg-light">
<div class="container py-5">
  <h1 class="mb-4">Reports <small class="text-muted">(<%= scope %>)</small></h1>
  <table class="table table-striped">
    <thead><tr><th>ID</th><th>Class</th><th>Teacher</th><th>Students</th><th>Tests</th><th>Grades</th></tr></thead>
    <tbody>
      <% report.forEach(r => { %>
        <tr>
          <td><%= r.classId %></td>
          <td><%= r.className %></td>
          <td><%= r.teacher || '-' %></td>
          <td><%= r.students %></td>
          <td><%= r.tests %></td>
          <td><%= r.grades %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
  <a href="javascript:history.back()" class="btn btn-link">Back</a>
</div>
</body></html>
