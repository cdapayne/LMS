<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Pre-Register – MD Technical School</title>
  <link rel="icon" href="<%= branding.favicon %>">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    :root{
      --primary-color: dodgerblue;
      --secondary-color: orange;
      --bg: #f4f6f9;
      --surface: #ffffff;
      --border: #e5e7eb;
      --muted: #6b7280;
      --focus: rgba(30,144,255,.35);
    }
    body{
      margin:0; background: var(--bg);
      font-family:"Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      color:#111827;
    }
    .pre-wrap{ min-height:100svh; display:grid; place-items:center; padding:1rem; }
    .pre-card{ width:100%; max-width:600px; background:var(--surface); border:1px solid var(--border); border-radius:16px; overflow:hidden; box-shadow:0 .8rem 1.6rem rgba(0,0,0,.08); }
    .pre-head{ padding:1.75rem 1.25rem; text-align:center; color:#fff; background:linear-gradient(120deg, rgba(30,144,255,.95), rgba(30,144,255,.85)), radial-gradient(1200px 500px at 10% -20%, rgba(255,165,0,.35), transparent); }
    .pre-head img{ height:64px; width:auto; margin-bottom:.5rem; object-fit:contain; }
    .pre-title{ font-weight:800; margin:0; font-size:1.5rem; }
    .pre-body{ padding:1.25rem; }
    .btn-primary{ background:var(--primary-color)!important; border-color:var(--primary-color)!important; }
    .btn-primary:hover{ background:#1e6edb!important; border-color:#1e6edb!important; }
    .form-control:focus{ border-color:var(--primary-color); box-shadow:0 0 0 .25rem var(--focus); }
  </style>
</head>
<body>
  <main class="pre-wrap">
    <section class="pre-card">
      <header class="pre-head">
        <img src="<%= branding.primaryLogo %>" alt="Logo">
        <h1 class="pre-title">Pre-Registration</h1>
      </header>
      <div class="pre-body">
        <% if (success) { %>
          <div class="alert alert-success">Thanks for your interest! We will contact you soon.</div>
        <% } else { %>
          <% if (error) { %>
            <div class="alert alert-danger"><%= error %></div>
          <% } %>
          <form method="post" action="/pre-register">
            <div class="mb-3">
              <label class="form-label">Name<span class="text-danger">*</span></label>
              <input name="name" class="form-control" required value="<%= formData.name || '' %>">
            </div>
            <div class="mb-3">
              <label class="form-label">Phone<span class="text-danger">*</span></label>
              <input name="phone" class="form-control" required value="<%= formData.phone || '' %>">
            </div>
            <div class="mb-3">
              <label class="form-label">Email (optional)</label>
              <input type="email" name="email" class="form-control" value="<%= formData.email || '' %>">
            </div>
            <div class="mb-3">
              <label class="form-label">Branch of Service<span class="text-danger">*</span></label>
              <input name="branch" class="form-control" required value="<%= formData.branch || '' %>">
            </div>
            <div class="mb-3">
              <label class="form-label">Region<span class="text-danger">*</span></label>
              <input name="region" class="form-control" required value="<%= formData.region || '' %>">
            </div>
            <div class="mb-3">
              <label class="form-label">Course<span class="text-danger">*</span></label>
              <input name="course" class="form-control" required value="<%= formData.course || '' %>">
            </div>
            <div class="mb-3">
              <label class="form-label">How did you hear about us?<span class="text-danger">*</span></label>
              <input name="referral" class="form-control" required value="<%= formData.referral || '' %>">
            </div>
            <div class="form-check mb-3">
              <input class="form-check-input" type="checkbox" name="consent" id="consent">
              <label class="form-check-label" for="consent">
                By checking this box, I consent to receive Marketing and Promotional Texts from MD Technical Schools (4 sms/month), Reply HELP for Info, STOP to Opt-out. Message and data rates may apply. For Privacy Policy click here.
              </label>
            </div>
            <button class="btn btn-primary w-100">Submit</button>
          </form>
        <% } %>
      </div>
    </section>
  </main>
</body>
</html>