<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Pre-Register – MD Technical School</title>
  <link rel="icon" href="<%= branding.favicon %>">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/css/theme.css">

</head>
<body>
  <button id="themeToggle" class="btn btn-sm btn-outline-secondary position-absolute top-0 end-0 m-3"></button>
  <main class="form-wrap">
    <section class="form-card">
      <header class="form-head">
        <img src="<%= branding.primaryLogo %>" alt="Logo">
        <h1 class="form-title">Pre-Registration</h1>
      </header>
      <div class="form-body">
        <% if (success) { %>
          <div class="alert alert-success">Thanks for your interest! We will contact you soon.</div>
        <% } else { %>
          <% if (error) { %>
            <div class="alert alert-danger"><%= error %></div>
          <% } %>
          <form method="post" action="/pre-register">
            <div class="mb-3">
              <label class="form-label">Name<span class="text-danger">*</span></label>
              <input name="name" class="form-control" required value="<%= formData.name || '' %>">
            </div>
            <div class="mb-3">
              <label class="form-label">Phone<span class="text-danger">*</span></label>
              <input name="phone" class="form-control" required value="<%= formData.phone || '' %>">
            </div>
            <div class="mb-3">
              <label class="form-label">Email (optional)</label>
              <input type="email" name="email" class="form-control" value="<%= formData.email || '' %>">
            </div>
            <div class="mb-3">
              <label class="form-label">Branch of Service<span class="text-danger">*</span></label>
              <input name="branch" class="form-control" required value="<%= formData.branch || '' %>">
            </div>
            <div class="mb-3">
              <label class="form-label">Region<span class="text-danger">*</span></label>
              <input name="region" class="form-control" required value="<%= formData.region || '' %>">
            </div>
            <div class="mb-3">
              <label class="form-label">Course<span class="text-danger">*</span></label>
              <input name="course" class="form-control" required value="<%= formData.course || '' %>">
            </div>
            <div class="mb-3">
              <label class="form-label">How did you hear about us?<span class="text-danger">*</span></label>
              <input name="referral" class="form-control" required value="<%= formData.referral || '' %>">
            </div>
            <div class="form-check mb-3">
              <input class="form-check-input" type="checkbox" name="consent" id="consent">
              <label class="form-check-label" for="consent">
                By checking this box, I consent to receive Marketing and Promotional Texts from MD Technical Schools (4 sms/month), Reply HELP for Info, STOP to Opt-out. Message and data rates may apply. For Privacy Policy click here.
              </label>
            </div>
            <button class="btn btn-primary w-100">Submit</button>
          </form>
        <% } %>
      </div>
    </section>
  </main>
  <script src="/js/theme.js"></script>
  <script>setupThemeToggle('themeToggle');</script>
</body>
</html>