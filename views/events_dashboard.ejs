<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Events Dashboard – School LMS</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <%- include('header', { user: user }) %>
  <div class="container py-4">
    <h2>Upcoming Events (next 3 weeks)</h2>
    <p class="text-muted">Class sessions: <%= classCount %> &nbsp;|&nbsp; Test due dates: <%= testCount %></p>
    <table class="table table-striped">
      <thead>
        <tr><th>Date</th><th>Time</th><th>Event</th><th>Class</th><th>Type</th></tr>
      </thead>
      <tbody>
        <% if (events.length === 0) { %>
          <tr><td colspan="5" class="text-center">No upcoming events</td></tr>
        <% } %>
        <% events.forEach(e => { %>
          <tr>
            <td><%= e.date %></td>
            <td><%= e.time %></td>
            <td><%= e.title %></td>
            <td><%= e.className %></td>
            <td><%= e.type %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
    <a href="javascript:history.back()" class="btn btn-link">Back</a>
  </div>
</body>
</html>